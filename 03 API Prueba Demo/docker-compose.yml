version: '3'

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./data_wrangling:/app/data_wrangling
    environment:
      - ENV=production